# name: Docker CI

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# permissions:
#   contents: read

# jobs:
#   build:
#     name: Build
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#       - name: Build Docker image
#         run: docker build -t fastapi-quick-starter:latest .
#       - name: Package image as artifact
#         run: docker save fastapi-quick-starter:latest -o fastapi-quick-starter.tar
#       - name: Upload image artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: fastapi-image
#           path: fastapi-quick-starter.tar
#           retention-days: 1

#   deploy:
#     name: Deploy (simulated)
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Download image artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: fastapi-image
#           path: .
#       - name: Load image
#         run: docker load -i fastapi-quick-starter.tar
#       - name: Simulate deploy
#         run: echo "Deploying fastapi-quick-starter:latest (simulated)"

#   summary:
#     name: Summary
#     runs-on: ubuntu-latest
#     needs:
#       - build
#       - deploy
#     if: always()
#     steps:
#       - name: Report pipeline summary
#         run: |
#           echo "Build result: ${{ needs.build.result }}"
#           echo "Deploy result: ${{ needs.deploy.result }}"
#           echo "Docker image fastapi-quick-starter:latest processed (simulated)"
